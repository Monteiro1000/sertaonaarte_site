<IfModule mod_headers.c>
    <FilesMatch "\.json$">
        Header set Content-Type "application/json; charset=utf-8"
    </FilesMatch>
</IfModule>

<FilesMatch "\.json$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
